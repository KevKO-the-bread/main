class Solution(object):
    def getConcatenation(self, nums):
        ans= []
        for i in range(2):
            for n in nums:
                ans.append(n)
        return ans
    
Liste=[1,1,1,2,2,3,4,5,6,6,6,6]
    
    
result = Solution().getConcatenation(Liste)  
print( result) 
